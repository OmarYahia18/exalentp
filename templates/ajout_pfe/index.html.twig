{% extends 'base.html.twig' %}

{% block title %}Hello AjoutPfeController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

    {{ form_start(form)}}
    <div class="row">
        <div class="col">{{ form_row(form.title) }}</div>
        <div class="col">{{ form_row(form.nom) }}</div>
        <div class="col">{{ form_row(form.Entreprise) }}</div>
    </div>
    {{ form_rest(form) }}
{% endblock %}
{% endblock %}
